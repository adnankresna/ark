/prep diana-okonkwo

I have a 1:1 with Diana on Monday. The agenda I'm walking in with is the auto-approve-under-$50 feature.

Per the `/prep` spec: read-only. Load the stakeholder file, recent ingestion that touches Diana's concerns (fraud, audit-trail, finance-team workload), any active hypotheses or decisions in the same space, and surface:

1. Last-touched date + cadence — am I on or off schedule?
2. What Diana cares about + the concerns/watch-outs that the auto-approve feature is going to trip.
3. The strongest customer evidence I have in support of the feature (cite specific artifacts).
4. The 2-3 questions I should expect Diana to push on, given her history.
5. Anything in the brain that conflicts with the feature proposal (a non-goal, a prior decision, a stakeholder dynamic).

Do NOT draft a hypothesis or a decision this turn. Do NOT update durable knowledge except for the `Last touched:` field on Diana's file (which is what `/prep` is allowed to maintain). I want the prep brief in the response itself.
