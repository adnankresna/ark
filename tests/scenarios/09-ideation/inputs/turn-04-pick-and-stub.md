# Stub the receipt-matching one

OK — the receipt-matching candidate is the one I want to take forward. Stub it out as a proposed hypothesis file under `hypotheses/`.

Requirements:

1. **Status: `proposed`.** Not `active`, not `promoted`, not `partially-validated`. I want this to be unambiguously a candidate I haven't committed to.
2. **Evidence rows tagged with path-typed provenance** that links to artifacts already in the brain (the three interviews under `ingestion/`, the insights entry, the competitor note, the CS monthly). Don't fabricate evidence we don't have.
3. **Explicit validation gates** under `Open questions / caveats:` — what would need to be true for this to get promoted? Be specific. Name observable thresholds (e.g., "promote when N additional customer interviews validate fuzzy-match design from segment X" — not "promote when there's more evidence"). Also name what would kill it.
4. **Don't touch the auto-categorization hypothesis.** It stays as-is (promoted, last-updated Feb 25).
5. **Don't draft a decision file.** This is a proposed hypothesis, not a decision. We're nowhere near deciding to build.

A short hypothesis file is better than a verbose one. Use the schema in `hypotheses/_SCHEMA.md`. Name the file something obvious — `receipt-matching.md` or `smart-receipt-matching.md` is fine.
